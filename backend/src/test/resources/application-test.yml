logging:
  file.name: ./log/backend-test.log

spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create
  mail:
    username: user
    password: admin
    host: 127.0.0.1
    port: 3025
    protocol: smtp